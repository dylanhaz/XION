// Import Game method from phaser for init new game
import { Game } from "phaser";
/**
 * Import configuration of game settings
 * 
 */
import { config } from "./config/gameConfig";

/**
 * Create new game
 */
const game = new Game(config);
